namespace CommonTools
{
    /// <summary>
    /// 数据库连接串web.config KEY值
    /// 不同的数据库，在此配置不同的枚举，与KEY值保持一致
    /// </summary>
    public enum DbConnectionType
    {
        Oauth_Data_Practice//设置默认数据库连接Key
    }
}
